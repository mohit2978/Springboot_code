spring.application.name=profile-demo

# These values are used when no profile is selected.
demo.environment=default
demo.message=Hello from the default profile
demo.database-url=jdbc:h2:mem:defaultdb
demo.feature-enabled=false
